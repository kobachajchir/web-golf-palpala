// TODO: Cambiar los roles de strings a estas constantes en toda la aplicación
// Uso: import { ROLES } from '../constants/roles'
//      if (user.role === ROLES.ADMIN) { ... }

export const ROLES = {
  ADMIN: 'admin',
  OWNER: 'owner',
  MEMBER: 'member',
  EMPLOYEE: 'employee',
};
